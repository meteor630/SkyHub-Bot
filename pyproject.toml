[project]
name = "skyhub-bot"
version = "1.0.0"
description = "Discord-бот SkyHub Aviation -- плагинная платформа сообщества с горячей перезагрузкой"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 120
target-version = "py312"
